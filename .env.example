# Dittofeed Environment Variables
DATABASE_URL=postgresql://dittofeed:password@localhost:5432/dittofeed
REDIS_URL=redis://localhost:6379
CLICKHOUSE_HOST=localhost
CLICKHOUSE_DATABASE=dittofeed
CLICKHOUSE_USERNAME=default
CLICKHOUSE_PASSWORD=
DEFAULT_WORKSPACE_ID=00000000-0000-0000-0000-000000000000
SECRET_KEY=YOUR_VERY_SECRET_KEY_HERE
DITTOFEED_API_KEY=YOUR_DITTOFEED_API_KEY_HERE
SESSION_SECRET=YOUR_SESSION_SECRET_HERE

# PostHog Environment Variables (if connecting to a self-hosted instance)
# These are typically configured within PostHog's own interface or its docker-compose setup.
# However, Dittofeed will need API access to it.
POSTHOG_API_KEY=YOUR_POSTHOG_API_KEY_HERE
POSTHOG_PROJECT_ID=YOUR_POSTHOG_PROJECT_ID_HERE
POSTHOG_HOST=http://localhost:8000

# Postgres Environment Variables (already set in docker-compose.yml, but can be listed for clarity)
# POSTGRES_DB=dittofeed
# POSTGRES_USER=dittofeed
# POSTGRES_PASSWORD=password

# Redis Environment Variables (already set in docker-compose.yml)
# No specific variables needed here for basic setup.

# If you are running services on different ports or hosts, adjust the URLs accordingly.
# For example, if your postgres is running on a different host:
# DATABASE_URL=postgresql://dittofeed:password@your_postgres_host:5432/dittofeed

# For production, ensure all secret keys are strong and unique.
# Do not commit your actual .env file with sensitive credentials to version control.
# Copy this file to .env and fill in your actual values.
# Example: cp .env.example .env
